apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kevin-backend-data
  namespace: kevin-agent
  labels:
    app.kubernetes.io/name: kevin-agent
    app.kubernetes.io/component: backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: standard  # Uncomment and set to your StorageClass
